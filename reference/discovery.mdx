---
title: "Discovery"
description: "Find OIDC provider metadata"
icon: "compass"
---

OIDC discovery publishes provider settings at a standard URL.

```text
GET https://protoauth.onrender.com/.well-known/openid-configuration
```

## Important fields

| Field | Description |
| --- | --- |
| `issuer` | Identifier that appears in issued tokens. |
| `authorization_endpoint` | Endpoint that starts sign-in. |
| `token_endpoint` | Endpoint that exchanges codes and refresh tokens. |
| `userinfo_endpoint` | Endpoint that returns user claims. |
| `jwks_uri` | Endpoint that returns signing keys. |
| `end_session_endpoint` | Endpoint that starts provider logout. |
| `scopes_supported` | Scopes the provider advertises. |
| `claims_supported` | Claims the provider advertises. |

## Example response

```json
{
	"issuer": "https://protoauth.onrender.com",
	"authorization_endpoint": "https://protoauth.onrender.com/o/authenticate",
	"token_endpoint": "https://protoauth.onrender.com/o/token",
	"userinfo_endpoint": "https://protoauth.onrender.com/o/userinfo",
	"revocation_endpoint": "https://protoauth.onrender.com/o/revoke",
	"jwks_uri": "https://protoauth.onrender.com/.well-known/jwks.json",
	"scopes_supported": [
		"openid",
		"profile",
		"email"
	],
	"response_types_supported": [
		"code"
	],
	"id_token_signing_alg_values_supported": [
		"RS256"
	],
	"claims_supported": [
		"iss",
		"sub",
		"aud",
		"exp",
		"iat",
		"name",
		"given_name",
		"family_name",
		"email"
	]
}
```